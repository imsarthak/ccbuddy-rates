
# Local-only Senco app-id for verification (never commit)
.senco.env

# Local-only Cuelinks publisher API token (never commit)
.cuelinks.env

# Local-only residential proxy URL, credentials inside (never commit)
.proxy.env
